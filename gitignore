# 의존성 파일 - 로컬에만 필요한 파일들
node_modules
.pnpm-store

# 빌드 결과물 - 배포용으로 생성되는 파일들
/build
/dist
/out
.cache
.parcel-cache

# 환경변수 파일 - 민감한 정보(API 키 등) 포함
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# 로그 파일
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.log

# 에디터 및 IDE 설정 파일
.vscode/        # VS Code 설정
.idea/          # WebStorm/IntelliJ 설정
*.sublime-project
*.sublime-workspace

# OS 관련 파일
.DS_Store       # macOS
Thumbs.db       # Windows